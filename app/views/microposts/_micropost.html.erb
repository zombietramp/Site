<li>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    发布 <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
  <% if current_user?(micropost.user) %>
    <%= link_to "删除", micropost, method: :delete,
                                     data: { confirm: "您确认删除吗？" },
                                     title: micropost.content %>
  <% end %>
</li>